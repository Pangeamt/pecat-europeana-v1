export const metadata = {
  title: "Sign in | PECAT-E",
  description: "Sign in to the PECAT-E translation quality assurance platform",
};

export default function LoginLayout({ children }) {
  return children;
}
